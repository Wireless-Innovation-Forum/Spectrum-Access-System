{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Registration response parameters returned to a CBSD",
  "type": "object",
  "required": [ "response" ],
  "properties": {
    "cbsdId": { "type": "string",
                "description": "Globally unique string generated by the SAS which identifies this CBSD." },
    "response": { "$ref": "file:Response.schema.json" },
    "measReportConfig": { "type": "array",
	                  "description": "Contains any measurement indication the SAS requests the CBSD to perform and report in a subsequent request.",
			  "items": { "enum": [ "EutraCarrierRssiNonTx", "EutraCarrierRssiAlways" ] }
    }
  }
}
